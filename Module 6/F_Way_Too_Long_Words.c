#include<stdio.h>
#include<string.h>
int main()
{
    int testcase;
    scanf("%d", &testcase);
    char s[101];
    for (int i = 0; i < testcase; i++)
    {
         scanf("%c", &s);
         int len = strlen(s);
    }
    
    
    return 0;
}